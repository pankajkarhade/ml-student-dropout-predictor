# ml-student-dropout-predictor
A machine learning project that predicts whether a student is at risk of dropping out using Logistic Regression. The model is trained on student demographic and academic data, with preprocessing, scaling, and feature engineering applied. A Streamlit web app allows users to input student details and get real-time dropout predictions.
